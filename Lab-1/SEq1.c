#include <stdio.h>
#include <stdlib.h>

struct node 
{
    int data;
    struct node *left, *right;
};

struct node *links
CreateList(int val){
    int n=10;
    if (val=NULL)
         node = (int*)malloc(+n*sizeof(int));

}